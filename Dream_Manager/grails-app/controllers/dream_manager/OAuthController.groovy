package dream_manager

class OAuthController {

	def callback () {
		
		// get token
		// trade for access token
		// save key record
		// render success page 
	
		if(success)	flash.message "Authorization Succeeded"
		else flash.message "Authorization Failed"
	}	
}
